import 'package:flutter/material.dart';

class BelomJadi extends StatelessWidget {
  const BelomJadi({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: new Text('BelumJadi'),
      ),
    );
  }
}
